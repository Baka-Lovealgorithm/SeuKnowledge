# 测试记录汇总

## 归档说明

本文档是 D:\code\KonwledgeRepo 下 15 份历史测试报告的精简汇总。原报告文件已删除，所有测试事实、数字、结论以本文为准。以下按主题域分组，组内按报告时间从新到旧排列；末尾附跨报告演进脉络与遗留事项清单。

---

## 一、解析与召回优化

### 【2026-08-24】`测试报告_元数据参与召回重排_用户手册5题.md`

改动：ES BM25 检索字段从仅 `content` 扩展为 `content^1.0 + title^2.0 + docName^0.5`；交叉编码器精排输入从纯正文改为「文档名 + 章节标题 + 正文」。测试设置：KB7 doc18 用户手册 828 chunks，5 题（DomainParticipant / IDL / QoS / Topic / Listener），A/B 基线 vs 改动后各跑一轮，每题新会话非流式，模型 deepseek-v4-flash-0731 + text-embedding-v4 + gte-rerank-v2。关键结果：加权准确率 90%→100%（+10pp），完全正确率 80%→100%（+20pp），平均耗时 44.5s→38.8s（-13%），重试次数 4→3，证据纯度明显改善（Q1/Q5 封面类 chunk 被排到后面）。结论：元数据参与精排有效，交叉编码器能识别章节语义，BM25 纳入 title 使长尾章节命中（Q3 QoS 策略覆盖面从 5 个扩到 21+ 个）。遗留：Q3 仍混入少量次要 chunk；Q2 混入 1 条分布式日志 chunk。

### 【2026-08-17】`测试报告_gte-rerank精排与召回改造.md`

改动：引入 gte-rerank-v2 交叉编码器精排（DashScope API），替换原有权重机制；RerankNode 按 sourceType 分组（CHUNK top4 + 业务/问答 top4）、用原始问题打分；召回层 CHUNK/BUSINESS/QA 各 top8，删除权重乘法。测试设置：KB7 纯 CHUNK 1057 条，5 题（Q1~Q5），模型 qwen-plus + text-embedding-v3 + gte-rerank-v2。关键结果：5 题全部正确，refs 均为 CHUNK=4；Q1/Q5 此前的内容级召回失败已解决（Q1 从"未找到"→完整环境要求，Q5 从"未找到"→三阶段排障框架）。重要发现：LlamaParse 解析的故障排查指南 3.3 节正文标题继承错乱（p46-49 标题为 3.1.1/3.2 等），Q5 只能依赖目录页。遗留：业务/问答配额未实测（KB7 无数据）；单元测试需 `-Dmaven.test.skip=true` 跳过。

### 【2026-08-17】`测试报告_LlamaParse重解析与召回优化.md`

改动：接入 LlamaParse 云端解析管线（pdf/docx→Markdown→递归分块）；新增 RecursiveChunkSplitter（800/120 跨页重叠+标题继承+跨页 carry）；向量模型切换 text-embedding-v4；召回逻辑改为 CHUNK top8 + BUSINESS/QA 合并 top5。测试设置：KB7 5 个 PDF 全量重解析（LlamaParse + 递归分块 + text-embedding-v4），纯 chunk 回归 5 题。关键结果：LlamaParse 转化质量好（标题层级完整、表格代码块保留、截图 OCR 并入正文）；全库 chunk 数 1708→1057（-38%）；5 题全部正确，Q1/Q5 此前 PDFBox 管线的召回失败已解决。遗留：故障排查指南 3.3 节未精确命中；Rerank 无来源配额；docx 未实测；LLAMA_CLOUD_API_KEY 未持久化。

### 【2026-08-20】`测试报告_PPTX解析.md`

改动：新增 PptxParserService + LlamaParse 截图支持（81 页整页截图 8 并发并行下载）；每页一个 chunk（不做 800 字符切分）；内容稀疏页识图补全（qwen3-omni-flash）；跨页页脚检测（跳过"臻融科技"页脚）。测试设置：KB8（新建），ZRDDS介绍V6.0.pptx（81 页/43MB），1 题问答验证。关键结果：81/81 截图下载成功（约 51 秒）；2 页触发识图补全；81 chunks 全部 INDEXED；标题提取 81 页全部正确（页脚残留 0/81）；性能表数字完整入库；单元测试 153 例全绿。遗留：PPTX 仅走 LlamaParse（无本地回退）；43MB 样本需 KB_FILE_MAX_SIZE≥60MB。

### 【2026-08-16】`测试报告_提示词加载与全量PDF向量化.md`

改动：全量 5 个 PDF 上传至 KB7 并向量化；提示词.md 加载为 Agent systemPrompt（臻融小智角色设定）。修复了 embedding 批大小超限（每批 ≤10 条）、识图模型 baseUrl 配置错误、@Async 线程池回退 SimpleAsyncTaskExecutor 三处缺陷。测试设置：KB7，5 个 PDF 全量向量化，5 题问答（C++ 手册）。关键结果：ES chunk 1708 条 + QA 111 + BUSINESS 102 = 1921 条；Q2/Q3/Q4 优秀，Q1/Q5 内容级召回失败（CHUNK 被 QA/BUSINESS 权重 1.2 挤出 top5）。后续完善（08-20）：提示词补充 ZRDDS 介绍文档映射 + 新增"回答约束"。遗留：抽取任务未覆盖 doc15/16/17/18。

---

## 二、问答链路准确性

### 【2026-08-25】`测试报告_A-B证据池扩容_10题.md`

改动：新增 SeuRecallProperties 配置化召回参数；A 组（基线）召回 8/精排 chunkTop 4 vs B 组（实验）召回 15/精排 chunkTop 6。测试设置：KB7 doc18 用户手册，10 题（含 5 道新增），每题新会话非流式，模型 deepseek-v4-flash。关键结果：人工核验两组均 9/10（Q3 QoS 是共同弱项，B 组更完整——8 策略 vs 6 策略）；B 组重试 0 次（A 组 1 次）；B 组平均断言 14.4（+41%）、答案字符 1017（+15.7%）、平均耗时 34.5s（+8.5%）；两组均受 TokenRythm 429 限流污染（A 5/10、B 3/10 自检 fail-open）。结论：采用 B 组配置（15/6），正确性持平、答案完整性显著提升、重试减少。遗留：429 限流需加题间间隔或换更稳上游；阶段二 429 可加自动重试。

### 【2026-08-23】`测试报告_KB7问答准确性回归_10题.md`

改动：纯 CHUNK 模式（无 QA/业务知识），KB7 含 6 文档 1144 chunks（含 PPTX 81 chunks）。测试设置：端口 18080，模型 TokenRythm deepseek-v4-flash-0731 + text-embedding-v4 + gte-rerank-v2，Agent verify=0.7/maxRetry=2，10 题每题新会话（PowerShell 5.1 首轮因 ASCII 编码中文乱码作废，改用 UTF-8 字节体重跑）。关键结果：完全正确率 8/10=80%，加权准确率 90%（10/10 无错误），幻觉率 0%，意图路由 0 误判；平均耗时约 37s（3 题触发 2 次重试，显著慢于旧 6s）。与历史对比：Q1 环境要求从"硬件无证据"→精确命中；Q5 收不到数据从"未找到"→框架正确但 3.3 正文仍未召回。遗留：故障排查指南 3.3 节标题错乱（历史已提出）；生成耗时偏高（建议换更快模型）；无 QA/业务知识源使长尾事实题易漏。

### 【2026-08-16】`测试报告_ZRDDS整链路准确性.md`

改动：全链路首次集成测试（上传→解析→分块→向量化→AI 抽取→全量审核→多源混合检索→问答生成）。修复了 embedding 批大小超限、识图 baseUrl 错误、@Async 线程池失效三处缺陷。测试设置：KB7，1 个 PDF（13 页 C++ 手册），16 题（含 QA 镜像/业务知识/文档事实），模型 qwen3-omni-flash（识图）+ qwen-plus（生成）+ text-embedding-v3。关键结果：识图 3 页零失败（彻底解决 tokenrhythm 504）；抽取 QA 准确率 100%、业务知识 87%（含 4 条缺陷条目如 RDDS_HOME 拼写错/ZettaScale 幻觉）；问答加权准确率 75%（10 完全+4 部分+2 错误），幻觉率 19%，意图路由误判 2/16。结论：识图链路稳定；抽取质量超预期但有世界知识注入与拼写错误传导；意图路由与综合题检索召回是主要短板。

---

## 三、自检与提示词

### 【2026-08-25】`测试报告_显式拒答与候选证据改造.md`

改动：重试耗尽后仍低于阈值 → 输出显式拒答文案（不再输出低分答案），同时将本轮证据写入 REFS 作为候选证据供用户自查；兜底文案收敛到 Defaults 常量消除三处重复定义。测试设置：仅跑改动相关单测 12 例（全绿），未跑全量回归。新测试集移除 Q6/Q7 镜像题，新增证据不足题（商业授权定价→预期拒答）与闲聊题（天气→预期 CHITCHAT）。遗留：拒答行为无条件生效（未引入 Agent 开关）；新测试集待下次全量回归执行。

### 【2026-08-24】`测试报告_P0B自检输出瘦身_Q5验证.md`

改动：自检阶段一/阶段二提示词改为严格 JSON 输出 + max_tokens 限制 + 关闭 thinking（通过 extraBody 传 `thinking:{"type":"disabled"}`）；新增 ExtractJsonParser.parseObject；LlmTrace 新增 maxTokens 重载。关键发现：deepseek-v4-flash 的 reasoning token 膨胀导致 max_tokens 触顶 → 空输出 → 门控静默失效（Q1~Q4 faithfulness=1.00 全是假象）。修复后（关思考 + max_tokens）：5 题后端平均耗时 15.7s（vs P0 基线 103.3s，-85%），faithfulness 5/5=1.00（真实逐断言判定），0 重试。追加：模型级「关闭思考」配置化（ModelConfig 新增 disableThinking/thinkingParams）；标题生成器复用关思考（out 从 4433→7 token，42s→0.9s）；VERIFY 独立模型配置（id8 TokenRythm deepseek-v4-flash）后 5 题回归平均 21.6s。遗留：compose/重写等问答链路调用仍带思考；VERIFY 可配更小模型。

### 【2026-08-24】`测试报告_P0提示词与事实一致性校验_用户手册5题.md`

改动：P0-1：few-shot 提示词（answer-compose.txt 加 2 示例）+ CitationValidator 引用越界校验（移除越界 `[n]`）；P0-2：双阶段自检——阶段二 VERIFY 模型拆原子断言逐条对照证据判 SUPPORTED/CONTRADICTED/UNSUPPORTED（新模板 answer-faithfulness.txt），faithfulness = SUPPORTED/断言总数，VERIFY_SCORE = min(阶段一, faithfulness)。测试设置：KB7 doc18 用户手册，5 题，模型 deepseek-v4-flash-0731。关键结果：5/5 完全正确，faithfulness 均值 0.96，重试 0 次（vs 基线 3 次），平均耗时 103.3s（+166%，双阶段自检固有代价）。门控修订迭代：证据片段 200 字时 Q2 过度敏感（faithfulness=0.47→重试 2 次 553s），改为全文后 0 重试 124s。遗留：耗时偏高可给 VERIFY 配更小模型；Q2 仍 2 条矛盾断言。

### 【2026-08-18】`测试报告_重试升级证据累积与缺失项反馈.md`

改动：重试轮证据累积（上轮精排证据并入候选池按 sourceType:chunkId 去重）；AnswerVerify 输出缺失信息 → QueryRewrite 定向改写（"上次检索已覆盖：<标题>；仍缺失：<缺失项>"）；证据上下文从"前 3 条×200 字"改为"全部证据标题+前 100 字"。测试设置：KB7 gte-rerank-v2 精排已上线，5 题（Q1~Q5），阈值 0.5 生产/0.9 强制触发。关键结果：阈值 0.5 下 5 题均未触发重试（验证打分更准）；阈值 0.9 强制触发时缺失项反馈精准（如"网络要求/JDK 依赖"定向改写），证据累积生效（日志确认"并入上轮证据 4 条"）；最终回归 5/5 通过。遗留：故障排查指南 3.3 节正文数据缺失；重试答案取舍（当前输出最后一轮，未保留最高分）；单元测试仍待修复。

---

## 四、并发与性能

### 【2026-08-25】`测试报告_标题异步化.md`

改动：标题生成从 ChatMessageStore.persistAnswer 事务内同步阻塞彻底剥离 → SessionTitleService 异步生成 + 条件补写（updateTitleIfAuto，WHERE title_auto=1 原子守卫）+ SETNX 防重。测试设置：单测 297/297 全绿；k6 30 并发闲聊压测（受 TokenRythm 429 阻隔，标题机制正交）；手动 rename 守卫场景 B 5/5 通过；KB7 回归 3 题正确。关键结果：persistAnswer 事务不再含标题 LLM 调用；同步/流式统一异步提交；手动 rename 后自动标题补写被原子跳过。遗留：标题 LLM 调用无超时上限（P2 加）；30 并发压测受 429 阻隔，收益无法量化。

### 【2026-08-25】`测试报告_P1并发性能.md`

改动：A2 上下文传播（ContextPropagator 快照 MDC+SseEmitter+TokenAccumulator+OTel）；B3 节点内并行（knn+bm25 并行、多查询并行、自检阶段一+阶段二并行）；B4 虚拟线程（Tomcat + qaTaskExecutor）；C5 全局信号量（公平 Semaphore 默认 32，tryAcquire 30s）。测试设置：单测 291/291 全绿；k6 5 VU×2 迭代压测（同步 ask，1 道简单题）；KB7 10 题回归。关键结果：k6 压测迭代 avg 81.0s→52.9s（-35%），p95 128s→79s（-38%），吞吐 +69%；10 题回归 10/10 正确；但成功率 70%（3/10 因并行自检放大上游并发→TokenRythm 429 限流）。遗留：并行自检放大 429 概率（可调低 KB_CONCURRENCY_LIMIT 至 8~16）；appendHistory Redis 脏缓存；SSE 流式路径未纳入压测。

### 【2026-08-25】`测试报告_会话落库并发安全.md`

改动：ChatSessionRepository 新增 @Lock(PESSIMISTIC_WRITE) findByIdForUpdate，ChatMessageStore.persistAnswer 事务内先对会话行加悲观锁再累加 messageCount/更新 lastMessageAt。测试设置：并发集成测试 ChatMessageStoreConcurrencyTest（真实 MySQL+Redis）3 用例；全量单测 284/284 通过。关键结果：2 线程并发无丢失更新（messageCount==4），消息 role 严格 USER→ASSISTANT 交替；4 线程并发 messageCount==8；跨会话并行互不影响。遗留：appendHistory（Redis 记忆缓存）在落库事务内执行，事务回滚时存在脏缓存隐患。

---

## 跨报告演进脉络

1. **解析层：PDFBox→LlamaParse→PPTX 全类型覆盖**。从 PDFBox 纯文本+识图（08-16）进化为 LlamaParse 云端 Markdown 解析+递归分块（08-17），chunk 质量显著提升（全库 -38%），再扩展至 PPTX 整页截图+识图补全+页脚跳过（08-20），形成 pdf/docx/pptx/txt/md 多格式统一管线。

2. **召回层：权重排序→交叉编码器精排→元数据增强→证据池扩容**。从 BUSINESS/QA 权重 1.2 > CHUNK 1.0 导致 chunk 被挤出（08-16），进化为 gte-rerank-v2 交叉编码器分组配额（08-17），再引入 BM25 title/docName 多字段+精排输入带章节标题（08-24），最后将召回 topK 8→15、精排 chunkTop 4→6（08-25），完整性与重试率同步改善。

3. **自检层：简单打分→证据累积+缺失反馈→双阶段事实一致性→瘦身提速**。从单一分数阈值重试（08-16），升级为重试轮证据累积+缺失项定向改写（08-18），再进化为双阶段自检（阶段一完整性+阶段二逐断言核对，08-24），最后通过关思考+严格 JSON+max_tokens 将自检耗时从 103s 压缩至 15.7s（08-24），并引入显式拒答替代低分输出（08-25）。

4. **提示词与幻觉控制：从零约束到多层防线**。从基础的 Agent 角色设定（08-16），逐步增加 few-shot 示例+引用越界程序化校验（08-24）、双阶段事实一致性逐断言核对（08-24）、回答约束"以文档原文为准不自行纠正"（08-20 补充），幻觉率从 19%（08-16）降至 0%（08-23 起持续保持）。

5. **并发可靠性：从事务内同步阻塞到异步化+虚拟线程+全局信号量**。从 @Async 回退 SimpleAsyncTaskExecutor（08-16 发现），到会话落库悲观锁防丢失更新（08-25），再到虚拟线程+节点内并行+全局信号量（08-25），最后标题生成彻底移出落库事务（08-25），形成从单线程正确性到并发性能的完整闭环。

6. **模型与上游稳定性：从 504 到 429 的持续对抗**。识图模型从 tokenrhythm kimi-k2.5 频繁 504 切换到 qwen3-omni-flash 零失败（08-16）；但 TokenRythm 中转的 deepseek 模型在并发场景下持续触发 429 上游限流（08-25 多份报告），成为系统吞吐天花板。

---

## 遗留事项清单

1. **故障排查指南 3.3 节标题错乱**（LlamaParse 解析后正文 chunk 标题继承为 3.1.1/3.2，无"3.3 收不到数据"字样）——需检查 LlamaParse markdown 输出或修复递归分块器标题识别，重新解析该文档。
2. **TokenRythm 上游 429 限流**——并行自检放大 LLM 并发后概率上升，建议评估集跑批加题间间隔、阶段二 429 加 1 次退避重试、或换更稳上游模型。
3. **VERIFY 用途未配独立小模型**——当前回退与 GENERATE 同模型，关思考后虽已够快，配更小模型可进一步降耗时。
4. **compose/重写等问答链路调用仍带思考**（输出量小暂不影响，但 reasoning 膨胀时有风险）。
5. **appendHistory Redis 脏缓存**——落库事务回滚时 Redis 已写入，需独立处理。
6. **标题 LLM 调用无超时上限**——后台线程执行不阻塞用户但占线程池，建议 P2 加超时。
7. **单元测试未修复**——DocumentParserServiceTest/KnowledgeBaseServiceTest 构造器与 LlamaParse/clearContent 改造不同步，启动需 `-Dmaven.test.skip=true`。
8. **docx 解析未实测**——管线已支持（上传→LlamaParse），需提供 docx 样本验证。
9. **业务/问答配额在 KB7 未实测**——KB7 纯 CHUNK 模式，other-top=4 逻辑仅代码走查确认，需在有数据的 KB 抽查。
10. **重试答案取舍**——当前重试用尽后输出最后一轮答案，未保留历轮最高分答案。
11. **抽取产物缺陷条目传导**——抽取阶段存在世界知识注入（ZettaScale 等）与拼写错误（RDDS_HOME/_ZRDDSCPINTERFACE），需增加术语拼写校验或与原文一致性过滤。
12. **综合事实题单源 top8 配额下遗漏**——如 Q10"支持哪些 OS"遗漏 Java 手册 p1 环境要求 chunk，可考虑 QueryRewrite 多路改写或证据不足时触发补充召回。
13. **LLAMA_CLOUD_API_KEY 未持久化**——后端重启需重新注入环境变量。
14. **显式拒答未引入 Agent 配置开关**——如后续需"可用性优先"场景可再加。
15. **SSE 流式路径未纳入 k6 压测**——当前仅覆盖同步 ask。
16. **@Async 平台线程池（8/32/256）仍承载流式问答**——若流式并发成瓶颈可评估虚拟线程化。